select  'hello';

select  'hello' as greeting;
select  'hello' as greeting;
select  'hello' as greeting;


-- show databases; -- All schemas from all databases are shown (based on current role).


-- show schemas in account; -- All schemas from all databases are shown (based on current role).